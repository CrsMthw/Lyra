<?xml version="1.0" encoding="utf-8"?>
<!-- DEBUG ONLY: the cassette painter's preview (never merged into the release manifest). -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <activity
            android:name="com.crsmthw.lyra.ui.cassette.CassettePreviewActivity"
            android:exported="true"
            android:theme="@style/Theme.Lyra" />
    </application>
</manifest>
